 # for jar2,val2 in state.items():
        #     if(jar1 != jar2):
        #         if(val1 + val2 <= jar1):
        #             new_state = state.copy()
        #             new_state[jar1] += val2
        #             calculateQuantityFromJars(new_state, quantity, pastStates)
        #         if(val2 + val1 <= jar2):
        #             new_state = state.copy()
        #             new_state[jar2] += val1
        #             calculateQuantityFromJars(new_state, quantity, pastStates)